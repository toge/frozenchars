#pragma once

/**
 * @file frozenchars.hpp
 * @brief 便利(全部入り)ヘッダ。
 *
 * glaze / json を除く全基本機能を集約する。
 * コンパイル負荷削減のため、個別のモジュールヘッダ
 * (frozenchars/mod/core.hpp 等) の利用も可能。
 */
#include "frozenchars/mod/all_basic.hpp"
